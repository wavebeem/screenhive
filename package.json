{
  "private": true,
  "scripts": {
    "postinstall": "install-app-deps",
    "make:js": "echo TODO",
    "prettify": "prettier --write 'app/src/**/*.js' app/style.css",
    "prestart": "npm run -s prettify && npm run -s make:js",
    "start": "electron app",
    "pack": "build --dir",
    "ico": "convert img/Screenhive-*px.png build/icon.ico",
    "icns": "./scripts/make-iconset",
    "dist:mac": "build --mac",
    "dist:win": "build --win"
  },
  "build": {
    "appId": "com.mockbrian.Screenhive",
    "mac": {
      "category": "public.app-category.utilities",
      "target": "dmg"
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": true
    }
  },
  "devDependencies": {
    "electron": "^1.8.1",
    "electron-builder": "^6.7.3",
    "eslint": "^3.19.0",
    "prettier": "1.9.2"
  }
}
